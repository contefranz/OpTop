% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/holdout.R
\name{.optop_holdout_prepare}
\alias{.optop_holdout_prepare}
\title{Shared held-out preparation}
\usage{
.optop_holdout_prepare(models, dtm_eval, baseline, ...)
}
\arguments{
\item{models, dtm_eval, baseline}{As in \code{\link[=optop_index_holdout]{optop_index_holdout()}}.}

\item{...}{Passed to \code{\link[=optop_fold_in]{optop_fold_in()}}.}
}
\value{
A list with the augmented dtm (\code{dtm_aug}), augmented baseline
probabilities and vocabulary (\code{pi_aug}, \code{vocab_aug}), the per-model
evaluation containers (\code{tp_eval}, theta folded in and phi augmented),
the aligned unaugmented counts (\code{dtm_aligned}), the models reordered to
the ascending topic grid (\code{models}), the topic grid (\code{K}) and the
training vocabulary (\code{vocab_tr}).
}
\description{
Validates the training objects, aligns the evaluation counts to the
training vocabulary, applies the out-of-support convention, folds the
document-topic weights in for every model, and returns the augmented
evaluation objects the held-out tools consume.
}
\keyword{internal}
