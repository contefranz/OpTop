% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration.R
\name{.optop_split_envelope}
\alias{.optop_split_envelope}
\title{Split the flattened envelope into per-document bins}
\usage{
.optop_split_envelope(bin_probs, bin_counts)
}
\arguments{
\item{bin_probs}{Numeric vector; the concatenated per-document bin
probabilities (kept-word fitted probabilities followed by the collapsed
min-bin mass).}

\item{bin_counts}{Integer vector; the number of bins of each document.}
}
\value{
A list of numeric vectors, one per document, each summing to 1.
}
\description{
The C++ core (\code{optimal_topic_core(..., return_envelope = TRUE)}) exports
the envelope as one concatenated numeric vector of bin probabilities plus
the number of bins per document; this helper splits it back into a list
with one bin-probability vector per document, the shape the calibration
routines consume.
}
\keyword{internal}
