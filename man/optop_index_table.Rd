% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discrepancies.R
\name{optop_index_table}
\alias{optop_index_table}
\title{OpTop goodness-of-fit indices table across a grid of LDA models}
\usage{
optop_index_table(
  models,
  dtm,
  metrics = c("se", "chisq", "deviance"),
  c = 1,
  macro = FALSE,
  reopt = "none",
  add_baseline_topic = FALSE,
  partition = NULL,
  baseline = NULL,
  level = c("document", "word"),
  block_size = NULL,
  ztest = FALSE,
  n_threads = 1L,
  min_null = NULL
)
}
\arguments{
\item{models}{A non-empty \code{list} of fitted \code{topicmodels::LDA} models (VEM or Gibbs),
typically a grid over different \eqn{K}; elements may be loader
functions returning a fit, materialized one at a time.}

\item{dtm}{A counts document–term matrix (documents × terms). Use
\code{Matrix::dgCMatrix} or a \code{quanteda::dfm} that represents raw counts
(not weighted), or an \code{\link[=optop_corpus]{optop_corpus()}} of count shards.}

\item{metrics}{Character vector selecting which indices to compute. Any subset of
\code{c("se", "chisq", "deviance")}. Default: \code{c("se","chisq","deviance")}.}

\item{c}{Positive scalar used inside \code{\link[=optop_make_partition]{optop_make_partition()}} to set the per-document
rare threshold \eqn{\tau_j = c / L_j}. Default: \code{1}, the value the paper
adopts and recommends when the deviance index is primary; use \code{c = 5}
for Pearson-primary work and report sensitivity to \code{c}.}

\item{macro}{Logical; if \code{TRUE}, also compute macro indices (equal-weight averages of
per-document indices). Default: \code{FALSE}.}

\item{reopt}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Re-optimization mode
forwarded to the underlying index; scheduled for removal before v1.0.0.
Use the default \code{"none"}.}

\item{add_baseline_topic}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Logical;
forwarded to the SE and chi-square indices to force non-negativity.
Scheduled for removal before v1.0.0. Default: \code{FALSE}.}

\item{partition}{Optional precomputed result from \code{\link[=optop_make_partition]{optop_make_partition()}}.
If \code{NULL}, it is computed internally from \code{models} and \code{dtm}.}

\item{baseline}{Optional precomputed result from \code{\link[=optop_make_baseline]{optop_make_baseline()}}. If \code{NULL},
it is computed internally from \code{dtm}.}

\item{level}{Character; aggregation level for the indices. \code{"document"} (default)
returns document-level micro/macro indices. \code{"word"} returns word-level
Micro-Word and Macro-Word indices.}

\item{block_size}{Integer or \code{NULL}; number of vocabulary terms to process at once
during word-level computation. Smaller values use less memory but may be slower.
If \code{NULL} (default), automatically chosen based on corpus size to target ~1.5 GB
memory usage. Only used when \code{level = "word"}.}

\item{ztest}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Logical; if \code{TRUE}, append
the in-sample Z-test statistics. The methodology reserves inference for
held-out evaluation; scheduled for removal before v1.0.0. Default: \code{FALSE}.}

\item{n_threads}{Integer; number of OpenMP threads used by the compiled
index engine and, when \code{partition} is computed internally, by the
partition kernel (default \code{1L}). Results are identical for any value.}

\item{min_null}{Nonnegative scalar; the null-discrepancy floor of the
document-level aggregation, applied uniformly across metrics and models
(the baseline discrepancy does not depend on \eqn{K}, so the same
documents are excluded at every grid point). The default \code{NULL}
resolves to the partition threshold constant \code{c}; \code{min_null = 0}
restores the pre-0.14.1 strict positivity rule. See the
\emph{Null-discrepancy floor} paragraph of \link{optop_index}.}
}
\value{
When \code{level = "document"}, a \code{data.frame} with one row per model and columns:
\itemize{
\item \code{K}: number of topics in the model.
\item \code{R2_SE}, \code{R2_chisq}, \code{R2_dev}: micro indices for the selected metrics.
\item \code{R2_SE_macro}, \code{R2_chisq_macro}, \code{R2_dev_macro}: macro indices when \code{macro = TRUE}.
\item \code{Z_SE}, \code{Z_chisq}, \code{Z_dev}, \code{pval_SE}, \code{pval_chisq}, \code{pval_dev}: Z-test
statistics and p-values when \code{ztest = TRUE}.
}

When \code{level = "word"}, a \code{data.frame} with columns:
\itemize{
\item \code{K}: number of topics in the model.
\item \code{R2_SE_micro_word}, \code{R2_chisq_micro_word}, \code{R2_dev_micro_word}: Micro-Word indices.
\item \code{R2_SE_macro_word}, \code{R2_chisq_macro_word}, \code{R2_dev_macro_word}: Macro-Word indices.
}

Missing columns are omitted when the corresponding metric is not requested.
}
\description{
Computes one or more OpTop goodness-of-fit indices as computed by \code{\link[=optop_index]{optop_index()}},
for a list of fitted topic models and returns a tidy table by number of topics \eqn{K}.
}
\details{
The function wraps \code{\link[=optop_index_se]{optop_index_se()}}, \code{\link[=optop_index_chisq]{optop_index_chisq()}}, and
\code{\link[=optop_index_deviance]{optop_index_deviance()}}. It reuses a harmonized support (via
\code{\link[=optop_make_partition]{optop_make_partition()}}) and a fixed baseline (via
\code{\link[=optop_make_baseline]{optop_make_baseline()}}) so results are directly comparable across \eqn{K}.
}
\section{Performance tips}{

\itemize{
\item To avoid recomputation, precompute \code{partition <- optop_make_partition(...)}
and \code{baseline <- optop_make_baseline(...)} once and pass them in.
\item Large vocabularies benefit from the \code{block} argument inside
\code{\link[=optop_make_partition]{optop_make_partition()}} (set there, not here).
}
}

\examples{
\dontrun{
library(quanteda)
library(OpTop)

K_grid = 2:10

# Tokenize the corpus
toks <- data_corpus_inaugural \%>\% 
  tokens(remove_punct = TRUE, 
         remove_symbols = TRUE, 
         remove_numbers = TRUE) \%>\% 
  tokens_tolower() \%>\% 
  tokens_remove(stopwords())
  
# Create the document-feature-matrix
mydfm <- dfm(toks)

# Estimate topic models via VEM
VEM_models <- lapply(
  K_grid, function(k) {
    topicmodels::LDA(x = mydfm, k = k)
  }
)

# 1) Quick run: compute partition/baseline internally
tab <- optop_index_table(
  models   = VEM_models,
  dtm      = mydfm,
  metrics  = c("se","deviance"),
  macro    = TRUE
)

# 2) Faster repeated runs: precompute and reuse partition/baseline
part <- optop_make_partition(models = VEM_models, dtm = mydfm, c = 1)
base <- optop_make_baseline(dtm = mydfm)
tab2 <- optop_index_table(
  models    = VEM_models,
  dtm       = mydfm,
  metrics   = c("se","chisq","deviance"),
  macro     = FALSE,
  partition = part,
  baseline  = base
)
}

}
\references{
Lewis, C. M. and Grossetti, F. (2026). Goodness-of-fit indices and
diagnostics for topic models. Working paper.

Lewis, C. M. and Grossetti, F. (2022). A statistical approach for optimal
topic model identification. \emph{Journal of Machine Learning Research},
23(58), 1--20. \url{https://jmlr.org/papers/v23/19-297.html}
}
\seealso{
\code{\link[=optop_index]{optop_index()}}, \code{\link[=optop_make_partition]{optop_make_partition()}}, \code{\link[=optop_make_baseline]{optop_make_baseline()}}
}
