% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration.R
\name{.optop_boot_null}
\alias{.optop_boot_null}
\title{Parametric bootstrap draw of the null Test 1 statistic}
\usage{
.optop_boot_null(
  probs,
  doc_lengths,
  n_boot,
  seed = NULL,
  n_threads = 1L,
  bin_probs = NULL,
  bin_counts = NULL,
  doc_offset = 0
)
}
\arguments{
\item{probs}{List of per-document bin-probability vectors, as produced by
\code{.optop_split_envelope()}; ignored when the flattened form is supplied.}

\item{doc_lengths}{Numeric vector of document lengths \eqn{N_j}, aligned
with the documents of the envelope.}

\item{n_boot}{Integer; number of bootstrap replicates.}

\item{seed}{Integer seed for the compiled core's RNG, or \code{NULL} to draw
one from the R session RNG.}

\item{n_threads}{Integer; number of OpenMP threads (default \code{1L}). Has no
effect on the result, only on wall time.}

\item{bin_probs, bin_counts}{Optional flattened envelope exactly as
exported by \code{optimal_topic_core()}; when supplied, \code{probs} is not
touched and no per-document list is ever materialized. The flattened
layout is identical to \code{unlist(probs)}, so both entry points draw the
same replicates under the same seed.}

\item{doc_offset}{Nonnegative scalar; the global index of the first
document of this call minus one. Every document's RNG stream is keyed
by \code{seed} and its GLOBAL index, so each document draws bit-identical
replicates whatever the sharding; summing the per-shard replicate
vectors reproduces the unsharded totals up to floating-point summation
order (a few ulp, since the grouping of the additions changes).
Default \code{0} (the unsharded call).}
}
\value{
A numeric vector of length \code{n_boot} with the null replicates
\eqn{T^\ast}{T*}.
}
\description{
Draws \code{n_boot} replicates of the statistic under the conditional
fitted-model null by simulating counts directly on the collapsed envelope
bins, \eqn{c_j \sim \mathrm{Multinomial}(N_j, p_j)}{c_j ~ Multinomial(N_j,
p_j)}, which is exact because a multinomial collapsed over bins is
multinomial on the collapsed probabilities. Each replicate accumulates
\eqn{T^\ast = \sum_j k_j \sum_b (c_{jb}/N_j - p_{jb})^2 / p_{jb}}{T* =
sum_j k_j sum_b (c_jb/N_j - p_jb)^2 / p_jb}, where the multiplier
\eqn{k_j} is the number of bins of document \eqn{j}, matching the
statistic's convention (\eqn{P_j + 1} with a min bin, \eqn{P_j} without).
}
\details{
The sampling runs in compiled code (\code{optop_boot_null_core()}), fused with
the Pearson reduction and parallelized over documents with OpenMP.

\strong{RNG contract.} R's RNG is not thread-safe, so the compiled core owns
its generator: every document draws from a private stream seeded
deterministically from \verb{(seed, document index)}, which makes the result
bit-identical for any \code{n_threads}. When \code{seed} is \code{NULL} it is drawn once
from the R session RNG, so \code{set.seed()} at the R level governs
reproducibility exactly as before. The draws are a different (equally
valid) stream than \code{rmultinom()}: calibrated p-values agree with the
pre-0.12.0 implementation up to Monte-Carlo noise, not bit for bit.
}
\keyword{internal}
