% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{optop-crossfit-methods}
\alias{optop-crossfit-methods}
\alias{print.optop_crossfit}
\alias{plot.optop_crossfit}
\title{Printing and plotting a cross-fitted result}
\usage{
\method{print}{optop_crossfit}(x, ...)

\method{plot}{optop_crossfit}(
  x,
  which = c("macro", "gains"),
  metric = NULL,
  epsilon = 0.01,
  alpha = 0.05,
  ...
)
}
\arguments{
\item{x}{An \code{\link[=optop_crossfit]{optop_crossfit()}} result.}

\item{...}{Ignored.}

\item{which}{\code{"macro"} (default) or \code{"gains"}.}

\item{metric}{For the gains view, the metric to draw (default: the
first evaluated metric).}

\item{epsilon, alpha}{The adequacy tolerance and one-sided level of
the gains view.}
}
\value{
\code{x} invisibly for the print method; the ggplot object,
invisibly, for the plot method (drawn as a side effect).
}
\description{
\code{print.optop_crossfit} summarizes an \code{\link[=optop_crossfit]{optop_crossfit()}} run: the
configuration, the epsilon-adequacy pick per metric at the default
tolerance, and the pooled summary table. \code{plot.optop_crossfit} draws
the same two views as \code{\link[=plot.optop_holdout]{plot.optop_holdout()}}, the pooled Macro curve
with its fold-replicate confidence band and the cross-fitted
adjacent-gains view; \code{epsilon} and \code{alpha} recompute the gains at
plot time.
}
