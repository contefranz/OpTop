% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discrepancies.R
\name{.optop_index_engine}
\alias{.optop_index_engine}
\title{Fused compiled evaluation of the discrepancy indices}
\usage{
.optop_index_engine(
  theta,
  phi,
  dtm,
  partition,
  pi_row,
  metrics,
  level,
  block_size = NULL,
  n_threads = 1L,
  do_model = TRUE,
  do_null = TRUE
)
}
\arguments{
\item{theta, phi}{The (possibly baseline-augmented) model matrices;
ignored when \code{do_model = FALSE}.}

\item{dtm}{The counts document-term matrix.}

\item{partition}{A partition from \code{\link[=optop_make_partition]{optop_make_partition()}}.}

\item{pi_row}{Baseline probabilities aligned to the dtm vocabulary.}

\item{metrics}{Character subset of \code{c("se", "chisq", "deviance")}.}

\item{level}{Aggregation level, \code{"document"} or \code{"word"}.}

\item{block_size}{Words per block at the word level (\code{NULL} for the
adaptive default); the document level derives its own block size.}

\item{n_threads}{OpenMP threads; never changes the result.}

\item{do_model, do_null}{Which sides to evaluate.}
}
\value{
A list with one entry per metric (\code{se}, \code{chisq}, \code{deviance}),
each a list of numeric vectors \code{model} and \code{null} (per document at the
document level, per word at the word level); metrics or sides that were
not requested are zero vectors.
}
\description{
Routes both aggregation levels of every index through the compiled
kernels of \code{src/index_core.cpp}, which reproduce the conventions of the
original block-vectorized R implementation exactly (per-metric eps
floors, collapsed-bin sums, decomposition into full, rare and min terms)
while computing all requested metrics, and optionally the
model-independent baseline side, in a single traversal with no
\verb{J x W} temporaries. Setting \code{do_model = FALSE} evaluates the baseline
side alone, which is how the grid-level \code{D_null} hoist is computed.
}
\keyword{internal}
