% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discrepancies.R
\name{.optop_index_null}
\alias{.optop_index_null}
\title{Baseline (no-topics) discrepancy for one metric}
\usage{
.optop_index_null(
  dtm,
  partition,
  pi_row,
  metric = c("se", "chisq", "deviance"),
  level = c("document", "word"),
  block_size = NULL,
  n_threads = 1L
)
}
\arguments{
\item{dtm}{The counts document-term matrix.}

\item{partition}{A partition from \code{\link[=optop_make_partition]{optop_make_partition()}}.}

\item{pi_row}{Baseline probabilities aligned to the dtm vocabulary, as
returned by \code{.optop_validate_alignment()}.}

\item{metric}{The discrepancy metric.}

\item{level}{Aggregation level, \code{"document"} or \code{"word"}.}

\item{block_size}{Number of vocabulary columns per processing block.}

\item{n_threads}{OpenMP threads used by the fused engine.}
}
\value{
The per-document \eqn{D_{null}}{D_null} vector when
\code{level = "document"} and the per-word null discrepancy vector when
\code{level = "word"}.
}
\description{
This quantity does not depend on the fitted model, only on the observed
counts, the harmonized partition and the global baseline, so grid
evaluations across \eqn{K} can compute it once and pass it to the index
workers via their \code{null_disc} argument. Formulas, eps floors and blocking
mirror the corresponding index workers exactly.
}
\keyword{internal}
