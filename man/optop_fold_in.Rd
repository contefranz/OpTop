% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{optop_fold_in}
\alias{optop_fold_in}
\alias{optop_fold_in.TopicModel}
\alias{optop_fold_in.textmodel_lda}
\alias{optop_fold_in.optop_warplda}
\alias{optop_fold_in.nlp_topic_fit}
\alias{optop_fold_in.default}
\title{Fold document-topic weights in for new documents}
\usage{
optop_fold_in(model, newdata, ...)

\method{optop_fold_in}{TopicModel}(model, newdata, ...)

\method{optop_fold_in}{textmodel_lda}(model, newdata, ...)

\method{optop_fold_in}{optop_warplda}(model, newdata, ...)

\method{optop_fold_in}{nlp_topic_fit}(model, newdata, ...)

\method{optop_fold_in}{default}(model, newdata, ...)
}
\arguments{
\item{model}{A fitted topic model supported by \code{\link[=optop_as_theta_phi]{optop_as_theta_phi()}}.}

\item{newdata}{A counts document-term matrix on the training vocabulary.}

\item{...}{Passed to the engine's fold-in routine.}
}
\value{
A numeric matrix of document-topic weights, one row per document
of \code{newdata}.
}
\description{
Adapter generic behind the held-out tools: given a topic model fitted on
the training sample and the counts of new documents on the same
vocabulary, return the folded-in document-topic weights
\eqn{\hat\theta_j}{theta_j} (rows summing to 1, aligned with the rows of
\code{newdata}), holding the trained topic-word distributions fixed. Every
method infers the weights from the new document and the trained global
objects only, the held-out-document reconstruction target of the paper.
}
\section{Methods (by class)}{
\itemize{
\item \code{optop_fold_in(TopicModel)}: topicmodels fits: the fold-in E step of
\code{topicmodels::posterior()} on the new documents.

\item \code{optop_fold_in(textmodel_lda)}: seededlda fits: refit the new documents with
the trained model held fixed (\code{textmodel_lda(newdata, model = .)}),
the package's supported prediction path.

\item \code{optop_fold_in(optop_warplda)}: WarpLDA wrappers: \code{model$transform()} on the
new documents.

\item \code{optop_fold_in(nlp_topic_fit)}: NLPstudio fits: recurse into the raw backend
fit stored in \code{model_object}.

\item \code{optop_fold_in(default)}: Unsupported classes fail with the list of
engines that support fold-in.

}}
\keyword{internal}
