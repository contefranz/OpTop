% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crossfit.R
\name{.optop_crossfit_gains}
\alias{.optop_crossfit_gains}
\title{Cross-fitted adjacent gains with fold-replicate bounds}
\usage{
.optop_crossfit_gains(x, metric, epsilon, alpha)
}
\arguments{
\item{x}{An \code{optop_crossfit} result.}

\item{metric}{One evaluated metric.}

\item{epsilon, alpha}{The adequacy tolerance and one-sided level.}
}
\value{
A list with the \code{gains} table (\code{K}, \code{succ_K}, \code{n}, \code{gain},
\code{se_fold}, \code{upper}), the selected \code{k_hat} (NA when no step
qualifies), and the configuration.
}
\description{
The cross-fitted analogue of \code{\link[=optop_gain_table]{optop_gain_table()}}: per-document
adjacent gains are pooled over all out-of-fold scores, and the
one-sided upper bound uses the fold-replicate standard error (the V
per-fold mean gains as replicates) instead of the per-document one.
The epsilon-adequacy rule then selects the first K whose upper bound
falls below \code{epsilon}.
}
\keyword{internal}
