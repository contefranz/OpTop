% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discrepancies.R
\name{.optop_index_deviance_impl}
\alias{.optop_index_deviance_impl}
\title{Worker behind optop_index_deviance()}
\usage{
.optop_index_deviance_impl(
  model,
  dtm,
  partition,
  baseline,
  macro,
  level,
  block_size,
  null_disc = NULL,
  n_threads = 1L,
  min_null = 0
)
}
\arguments{
\item{model}{A single fitted \code{topicmodels::LDA} model (VEM or Gibbs), or
a loader function returning one (materialized on demand).}

\item{dtm}{A counts document–term matrix (documents × terms). Use \code{Matrix::dgCMatrix}
or a \code{quanteda::dfm} that represents raw counts (not weighted), or an
\code{\link[=optop_corpus]{optop_corpus()}} of count shards for corpora past the size of a single
matrix (shards stream one at a time; per-document results are
bit-identical to an unsharded run).}

\item{partition}{Result from \code{\link[=optop_make_partition]{optop_make_partition()}}, computed on a DTM that is
\emph{aligned} to the model vocabulary (same features and order).}

\item{baseline}{Result from \code{\link[=optop_make_baseline]{optop_make_baseline()}}, computed on the same aligned
counts DTM used for \code{partition}.}

\item{macro}{Logical; if \code{TRUE}, also compute the Macro index (equal-weight average of
the per-document indices). Default: \code{FALSE}.}

\item{level}{Character; aggregation level for the index. \code{"document"} (default) computes
document-level indices aggregated across words. \code{"word"} computes word-level indices
aggregated across documents.}

\item{block_size}{Integer or \code{NULL}; number of vocabulary terms to process at once
during word-level computation. Smaller values use less memory but may be slower.
If \code{NULL} (default), automatically chosen based on corpus size to target ~1.5 GB
memory usage. Only used when \code{level = "word"}.}

\item{null_disc}{Optional model-independent baseline discrepancy
(per-document \eqn{D_{null}}{D_null} when \code{level = "document"}, per-word
SST when \code{level = "word"}), as computed by \code{.optop_index_null()}, so
that grid evaluations across \eqn{K} reuse it instead of recomputing it
for every model.}

\item{n_threads}{Integer; number of OpenMP threads used by the compiled
index engine (default \code{1L}). Results are identical for any value; only
wall time changes. The package website article on very large corpora
covers thread and BLAS configuration at scale.}

\item{min_null}{Nonnegative scalar; the null-discrepancy floor of the
document-level aggregation. Documents with baseline discrepancy
\eqn{D_j(\mathrm{null}) < \mathrm{min\_null}}{D_j(null) < min_null}
are excluded from the Micro and Macro aggregations and carry an \code{NA}
document-level index. The default \code{NULL} resolves to the partition
threshold constant \code{c}; \code{min_null = 0} restores the pre-0.14.1 strict
positivity rule. See the \emph{Null-discrepancy floor} paragraph in Details.}
}
\value{
The same list the exported wrapper documents.
}
\description{
See \code{.optop_index_se_impl()} for the role of these workers and the
meaning of \code{null_disc}.
}
\keyword{internal}
