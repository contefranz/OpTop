% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{optop_model}
\alias{optop_model}
\title{Build an OpTop model from bare weight matrices}
\usage{
optop_model(theta, phi)
}
\arguments{
\item{theta}{Numeric matrix, documents by topics: row \code{j} holds the
topic weights of document \code{j} and sums to 1. Row names are the
document identifiers, required and unique.}

\item{phi}{Numeric matrix, topics by words: row \code{k} holds the word
distribution of topic \code{k} and sums to 1. Column names are the terms,
required and unique, in the same order as the columns of the
document-term matrix the model was estimated on.}
}
\value{
An object of class \code{optop_theta_phi}: a list with elements
\code{theta}, \code{phi}, \code{K} (the number of topics), \code{docs}, and \code{terms}.
}
\description{
Wrap a document-topic matrix and a topic-word matrix into the model
object every OpTop tool accepts. This is the public entry point for
engines the package has no adapter for: estimate the two matrices with
any software, hand them to \code{optop_model()}, and the result flows through
\code{\link[=optop_select]{optop_select()}}, the discrepancy indices, and the grid summaries like
any supported fit.
}
\details{
The contract is validated on construction: \code{theta} is documents by
topics and \code{phi} is topics by words, both nonnegative with rows summing
to 1 (tolerance \code{1e-6}); \code{theta} needs one unique document identifier
per row in its row names and \code{phi} one unique term per column in its
column names, because OpTop aligns by name and never by position.

One limitation follows from having no fitted engine behind the object:
an \code{optop_model()} cannot fold new documents in, so the held-out tools
(\code{\link[=optop_index_holdout]{optop_index_holdout()}} and everything built on it), which must adapt
the model to unseen documents, do not accept it. Use the engine's own
object there instead.
}
\examples{
theta <- matrix(c(0.7, 0.3,
                  0.2, 0.8), 2, 2, byrow = TRUE,
                dimnames = list(c("doc1", "doc2"), NULL))
phi <- matrix(c(0.5, 0.3, 0.2,
                0.1, 0.2, 0.7), 2, 3, byrow = TRUE,
              dimnames = list(NULL, c("alpha", "beta", "gamma")))
m <- optop_model(theta, phi)
m

}
\seealso{
\code{\link[=optop_as_theta_phi]{optop_as_theta_phi()}} for the adapter generic behind the
supported engines, \code{\link[=optop_warplda]{optop_warplda()}} for text2vec WarpLDA fits.
}
