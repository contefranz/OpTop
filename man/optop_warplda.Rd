% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{optop_warplda}
\alias{optop_warplda}
\title{Wrap a text2vec WarpLDA fit for OpTop}
\usage{
optop_warplda(model, doc_topic_distr)
}
\arguments{
\item{model}{A fitted \code{text2vec::LDA} (WarpLDA) R6 object.}

\item{doc_topic_distr}{The document-topic probability matrix returned by
\code{model$fit_transform()} on the corpus under evaluation (rows are
documents and carry the document identifiers, columns are topics, rows
sum to 1).}
}
\value{
An object of class \code{optop_warplda} holding the pair; its
\code{optop_as_theta_phi()} method exposes theta and phi with the usual
contract.
}
\description{
text2vec's WarpLDA sampler returns the document-topic matrix from
\code{fit_transform()} without retaining it in the model object, while the
topic-word distribution stays available as an active binding. This helper
bundles the two into a light object accepted by every OpTop tool that
takes fitted topic models (\code{\link[=optimal_topic]{optimal_topic()}}, \code{\link[=optop_make_partition]{optop_make_partition()}},
the index family, \code{\link[=optop_index_holdout]{optop_index_holdout()}} and \code{\link[=optop_moment_test]{optop_moment_test()}}).
}
\examples{
\dontrun{
lda <- text2vec::LDA$new(n_topics = 10)
theta <- lda$fit_transform(dtm, n_iter = 1000)
fit <- optop_warplda(lda, theta)
part <- optop_make_partition(list(fit), dtm)
}

}
\references{
Lewis, C. M. and Grossetti, F. (2026). Goodness-of-fit indices and
diagnostics for topic models. Working paper.
}
